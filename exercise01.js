// Função soma

// Criar uma função sum que recebe dois valores numéricos inteiros como parâmetros
// e retorna a soma destes valores.
function sum(number1, number2) {
    return number1 + number2;
}

let result = sum(12, 10);
